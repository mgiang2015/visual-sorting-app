import Head from 'next/head';
import Image from 'next/image';
import styles from '../styles/Home.module.css';
import Header from '../components/header';
import { useState, useEffect } from 'react';

export default function Home() {
  const [sortType, setSortType] = useState("Bubble"); // Default bubble sort
  const [elementNum, setElement] = useState(0);
  return (
    <div className={styles.container}>
      <Head>
        <title>Create Next App</title>
        <meta name="description" content="Generated by create next app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <Header setSortType={setSortType} setElement={setElement} />
      <h1>State Update!</h1>
      <h3>Sorting type picked: {sortType}</h3>
      <h3>Number of elements picked: {elementNum}</h3>
    </div>
  )
}
